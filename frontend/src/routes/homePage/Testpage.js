// Temporary Map Test Page (TestPage.jsx)
import React from 'react';
import Map from '../../components/map/map';

const Testpage = () => (
  <div style={{ height: '100vh', width: '100%' }}>
    <Map />
  </div>
);

export default Testpage;
